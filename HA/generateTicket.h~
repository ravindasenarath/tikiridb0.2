
/*struct ticket {
		 int ticketId;
                 char username[20];
                 char date[10];
                 int validityPeriod;
                 char sk2[16];
                 char attr[40][15];
                 char duration[40][10];
              }; 
              
typedef struct ticket USR_TICKET;*/

typedef struct ticket {	/* test field */
	int ticketId;
	char username[20];
        char date[10];
        int validityPeriod;
        char sk2[16];
        char attr[40][15];
        char duration[40][10];
} USR_TICKET;

USR_TICKET generateUsrTicket (char usrname[], char * sk2, int count);
